name: Build and test

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.0.x

    - name: Restore
      run: dotnet restore ./src/DevHunter.sln

    - name: Build
      run: dotnet build ./src/DevHunter.sln --no-restore

    - name: Test
      run: dotnet test ./src/DevHunter.Services.Tests/DevHunter.Services.Tests.csproj --no-build --no-restore
